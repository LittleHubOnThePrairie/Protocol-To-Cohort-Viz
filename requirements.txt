# PTCV runtime dependencies
# Install with: pip install -r requirements.txt

# Storage (PTCV-29)
minio>=7.2.0
pyarrow>=14.0.0
duckdb>=0.10.0

# PDF extraction (PTCV-19)
pdfplumber>=0.11.0
camelot-py[base]>=0.11.0
tabula-py>=2.9.0
pandas>=2.0.0

# XML parsing (PTCV-19 CTR-XML)
lxml>=5.0.0

# HTTP (PTCV-18 protocol download)
requests>=2.31.0

# LLM retemplating (PTCV-60)
anthropic>=0.40.0

# SDTM XPT serialization (PTCV-22)
pyreadstat>=1.3.0

# UI (PTCV-32 Streamlit MVP)
streamlit>=1.28.0
streamlit-code-diff>=0.1.0
plotly>=5.0.0
kaleido>=0.2.1
